import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/common/Card';

export default function ActivityPage() {
  return (
    <div className="space-y-lg">
      <div>
        <h1 className="text-3xl font-bold text-neutral-900 dark:text-neutral-50">Activity</h1>
        <p className="text-neutral-600 dark:text-neutral-400 mt-xs">
          Recent vault transactions and events
        </p>
      </div>

      <Card>
        <CardHeader>
          <CardTitle>Transaction History</CardTitle>
          <CardDescription>All payments and policy updates</CardDescription>
        </CardHeader>
        <CardContent>
          <p className="text-neutral-600 dark:text-neutral-400">No activity yet.</p>
        </CardContent>
      </Card>
    </div>
  );
}
